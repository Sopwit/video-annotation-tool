# Tests

This project uses the Node.js built-in test runner for lightweight unit tests.

## Run

```bash
npm run test
```

## Current Scope

- Utility-level unit tests (`tests/unit/*`).

## Next Steps

- Add component-level tests for key workflows (save/load, timeline seek, shortcuts).
- Add end-to-end smoke tests for Electron app startup and core annotation flow.
